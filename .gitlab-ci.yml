# This file is generated by GitLab CI
# Deployed Tests:
#   script:
#     - ssh root@project.unitar.my 'cd /var/www/; git pull; composer install; Console/cake schema create'
#   tags:
#   except:
#   - tags
# Staging:
#   script:
#   - ssh root@project.unitar.my 'cd /var/www/; git pull; composer install; Console/cake schema create'
#   type: deploy
#   tags:
#   only:
#   - develop
#   Production:
#     script:
#     - ssh root@project.unitar.my 'cd /var/www/; git pull; composer install; Console/cake schema create'
#     type: deploy
#     tags:
#     only:
#     - master
